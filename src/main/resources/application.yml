server:
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://test-db-mysql.ns-7o35k37x.svc:3306/school_admin?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: jwj9n8l4
    driver-class-name: com.mysql.cj.jdbc.Driver
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

jwt:
  # Production: set JWT_SECRET env var to a cryptographically strong random secret (>= 32 chars).
  # Development fallback is provided below — never use it in production.
  secret: ${JWT_SECRET:7a8b9c0d1e2f3a4b5c6d7e8f9a0b1c2d3e4f5a6b7c8d9e0f1a2b3c4d5e6f7a8b}
  access-token-expiration: 7200000
  refresh-token-expiration: 604800000

knife4j:
  enable: true
  openapi:
    title: School Admin API
    description: 学校后台管理系统接口文档
    version: 1.0.0
